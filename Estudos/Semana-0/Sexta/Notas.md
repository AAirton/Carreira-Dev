1. git status --short
 - Visualizar **arquivos unstaged** (que não foram selecionados)

2. git diff --name-only --staged
- Vizualizar **arquivos staged** (que foram selecionados)

3. git add <file>
- Selecionar um arquivo para fazer o **commit**.
Por exemplo:
_git add Estudos/Semana-0/Resumo.md_

4. git commit -m "<mensagem>"
- Realizar o commit com a respectiva **mensagem**.
Por exemplo:
_git commit -m "Adicionando arquivos alterados"_

5. git push
- Realizar **push dos commits**







